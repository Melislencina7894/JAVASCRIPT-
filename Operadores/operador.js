/*Operador de asignacion
Simbolo	Descripcion
=	operador de asignacion
Operadores de comparacion
Simbolo	Descripcion
==	igual que
===	estrictamente igual que
> or >= or >==	mayor o mayor igual que
< or <= or <==	menor o menor igual que
!= or !==	diferente que
Operadores aritmeticos
Simbolo	Descripcion
+	operador suma este se utiliza para concatener dos cadenas de texto.
-	operador resta
*	operador de multicacion
/	operador de division
%	operador de modulo
**	operador de potenciacion
tambien se les conoce como operadores binarios. por que toman dos valores y generan un resultado.

Operadores logicos
Simbolo	Descripcion
!	NOT niega un valor
&&	AND
||	OR*/ 

//Operadores binarios:
3 + 2 //Suma
50 - 10 // Resta
10 * 20 //Multiplicación
20 / 2 //División

"Melisa" + "Lencina" //concatenación de strings

/*Operadores unitarios:
!false //negamos que esto es falso = true 

//Operadores de asignación:
var a = 1; //Asignamos un valor a la variable

//Operadores para comparar:
3 == "3"; //Compara los valores y devuelve "true" en este caso

3 === "3"; //Compara y valida los tipos y valores. Devuelve "falso" en este caso

5 < 3 //Compara y valida si el 5 es menor a 3. False 
5 > 3 //Compara y valida si el 5 es mayor a 3. True
5 <= 6 //Compara y valida si el 5 es menor o igual al 6. True 
5 >= 6 //Compara y valida si el 5 es mayor o igual al 6. False 

a && b //Valida si ambas variables son verdaderas para que se cumpla la condición. Si a es verdadera y también b. 
a || b //Aquí se cumple la condición si alguna de las dos variables es verdadera. Si a es verdad o b es verdadera 
//se cumple la condición

var edad = 40
edad++ //Incrementa el valor en 1
//40

edad += 2 //Incrementa el valor por 2
//43 


// LOGICOS

&& // AND, Y, a && b : a y b
|| // OR, O, a || b : a o b
! // NOT, Negación, a = true : !a = false

// LOGICOS A NIVEL DE BITS

<< // Desplazamiento a la izquierda  a << b
>> // Desplazamiento a la derecha  a >> b
>>> // Desplazamiento a la derecha sin signo  a >>> b
& // AND  a & b
^ // XOR  a ^ b
| // OR  a | b
~ // NOT  a | b```  */